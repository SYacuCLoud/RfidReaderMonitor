<Window x:Class="RfidReaderMonitor.Views.CollectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:RfidReaderMonitor.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:CollectorViewModel}"
        Title="RFID Reader Monitor - 수집 모드" Height="760" Width="1240" MinWidth="900" MinHeight="560"
        FontFamily="Malgun Gothic" FontSize="12.5" Background="#F4F5F7">

    <Window.Resources>
        <Style x:Key="HostTile" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1,1,1,4"/>
            <Setter Property="BorderBrush" Value="#C43D3D"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="Width" Value="300"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Link}" Value="{x:Static vm:HostLink.Live}">
                    <Setter Property="BorderBrush" Value="#2E9E5B"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Link}" Value="{x:Static vm:HostLink.Stale}">
                    <Setter Property="BorderBrush" Value="#D98E04"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="LinkBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Padding" Value="6,1"/>
            <Setter Property="Background" Value="#C43D3D"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Link}" Value="{x:Static vm:HostLink.Live}">
                    <Setter Property="Background" Value="#2E9E5B"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Link}" Value="{x:Static vm:HostLink.Stale}">
                    <Setter Property="Background" Value="#D98E04"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ReaderStateText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#8A94A6"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding State}" Value="PRESENT">
                    <Setter Property="Foreground" Value="#2E9E5B"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="뽑힘">
                    <Setter Property="Foreground" Value="#C43D3D"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="사용불가">
                    <Setter Property="Foreground" Value="#C43D3D"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="SumCaption" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#777"/>
        </Style>
        <Style x:Key="SumValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1B2A41"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="White" BorderBrush="#D8DBE0" BorderThickness="0,0,0,1" Padding="10,8">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <TextBlock Text="수집 모드" FontSize="15" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,14,0"/>
                    <TextBlock Text="{Binding ListenText}" VerticalAlignment="Center" Foreground="#444" Margin="0,0,14,0"/>
                    <TextBlock Text="{Binding ClientCount, StringFormat='접속 {0}'}" VerticalAlignment="Center" Foreground="#444"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="CSV 폴더" Command="{Binding OpenCsvFolderCommand}"/>
                    <Button Content="이벤트 지우기" Command="{Binding ClearEventsCommand}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Border DockPanel.Dock="Bottom" Background="#ECEEF1" BorderBrush="#D8DBE0" BorderThickness="0,1,0,0" Padding="10,4">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#333" TextTrimming="CharacterEllipsis"/>
        </Border>

        <!-- 합계 -->
        <Border DockPanel.Dock="Top" Padding="10,8,10,0">
            <WrapPanel>
                <Border Background="White" BorderBrush="#D8DBE0" BorderThickness="1" CornerRadius="6" Padding="12,6" Margin="0,0,8,6" MinWidth="120">
                    <StackPanel>
                        <TextBlock Text="연결된 PC" Style="{StaticResource SumCaption}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding LiveHosts}" Style="{StaticResource SumValue}"/>
                            <TextBlock Text="{Binding Hosts.Count, StringFormat=' / {0}'}" Style="{StaticResource SumValue}" Foreground="#999"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <Border Background="White" BorderBrush="#D8DBE0" BorderThickness="1" CornerRadius="6" Padding="12,6" Margin="0,0,8,6" MinWidth="120">
                    <StackPanel>
                        <TextBlock Text="태그 감지 중" Style="{StaticResource SumCaption}"/>
                        <TextBlock Text="{Binding TotalPresent}" Style="{StaticResource SumValue}"/>
                    </StackPanel>
                </Border>
                <Border Background="White" BorderBrush="#D8DBE0" BorderThickness="1" CornerRadius="6" Padding="12,6" Margin="0,0,8,6" MinWidth="120">
                    <StackPanel>
                        <TextBlock Text="오늘 등장" Style="{StaticResource SumCaption}"/>
                        <TextBlock Text="{Binding AppearToday}" Style="{StaticResource SumValue}" Foreground="#1E7E45"/>
                    </StackPanel>
                </Border>
                <Border Background="White" BorderBrush="#D8DBE0" BorderThickness="1" CornerRadius="6" Padding="12,6" Margin="0,0,8,6" MinWidth="120">
                    <StackPanel>
                        <TextBlock Text="오늘 제거" Style="{StaticResource SumCaption}"/>
                        <TextBlock Text="{Binding RemoveToday}" Style="{StaticResource SumValue}" Foreground="#B03A2E"/>
                    </StackPanel>
                </Border>
                <Border Background="White" BorderBrush="#D8DBE0" BorderThickness="1" CornerRadius="6" Padding="12,6" Margin="0,0,8,6" MinWidth="320">
                    <StackPanel>
                        <TextBlock Text="마지막 이벤트" Style="{StaticResource SumCaption}"/>
                        <TextBlock Text="{Binding LastEventText}" FontSize="14" FontWeight="SemiBold" Foreground="#1B2A41" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </Border>
            </WrapPanel>
        </Border>

        <Grid Margin="10,4,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto" MaxHeight="360"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="감시 PC" Style="{StaticResource Head}" Margin="0,0,0,4"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Hosts}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource HostTile}">
                                <StackPanel>
                                    <DockPanel>
                                        <Border DockPanel.Dock="Right" Style="{StaticResource LinkBadge}" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding LinkText}" Foreground="White" FontSize="11"/>
                                        </Border>
                                        <TextBlock Text="{Binding Host}" FontSize="18" FontWeight="Bold" Foreground="#222" TextTrimming="CharacterEllipsis"/>
                                    </DockPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,6">
                                        <TextBlock Text="{Binding AgeText, StringFormat='하트비트 {0}'}" FontSize="11" Foreground="#888"/>
                                        <TextBlock Text="{Binding Version, StringFormat='  · v{0}'}" FontSize="11" Foreground="#888"/>
                                        <TextBlock Text="{Binding Remote, StringFormat='  · {0}'}" FontSize="11" Foreground="#AAA"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                        <TextBlock Text="{Binding OnlineReaders}" FontSize="20" FontWeight="Bold" Foreground="#1B2A41"/>
                                        <TextBlock Text="{Binding ReaderCount, StringFormat='/{0} 리더'}" FontSize="12" Foreground="#666" VerticalAlignment="Bottom" Margin="2,0,14,3"/>
                                        <TextBlock Text="{Binding PresentReaders}" FontSize="20" FontWeight="Bold" Foreground="#2E9E5B"/>
                                        <TextBlock Text="감지 중" FontSize="12" Foreground="#666" VerticalAlignment="Bottom" Margin="4,0,14,3"/>
                                        <TextBlock Text="{Binding AppearToday}" FontSize="20" FontWeight="Bold" Foreground="#1E7E45"/>
                                        <TextBlock Text="등장" FontSize="12" Foreground="#666" VerticalAlignment="Bottom" Margin="4,0,10,3"/>
                                        <TextBlock Text="{Binding RemoveToday}" FontSize="20" FontWeight="Bold" Foreground="#B03A2E"/>
                                        <TextBlock Text="제거" FontSize="12" Foreground="#666" VerticalAlignment="Bottom" Margin="4,0,0,3"/>
                                    </StackPanel>
                                    <ItemsControl ItemsSource="{Binding Readers}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <DockPanel Margin="0,1">
                                                    <TextBlock DockPanel.Dock="Right" Text="{Binding State}" Style="{StaticResource ReaderStateText}" Margin="8,0,0,0"/>
                                                    <TextBlock FontSize="11.5" TextTrimming="CharacterEllipsis">
                                                        <Run Text="{Binding Alias, Mode=OneWay}" FontWeight="SemiBold"/>
                                                        <Run Text="{Binding Serial, Mode=OneWay}" FontFamily="Consolas" Foreground="#666"/>
                                                        <Run Text="{Binding Uid, Mode=OneWay}" FontFamily="Consolas" Foreground="#1B2A41"/>
                                                    </TextBlock>
                                                </DockPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Text="{Binding LastEventText}" FontSize="11" Foreground="#555" Margin="0,6,0,0" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <GridSplitter Grid.Row="2" HorizontalAlignment="Stretch" Background="Transparent"/>

            <DockPanel Grid.Row="3">
                <TextBlock DockPanel.Dock="Top" Text="통합 이벤트" Style="{StaticResource Head}" Margin="0,4,0,4"/>
                <DataGrid ItemsSource="{Binding Events}" SelectedItem="{Binding SelectedEvent}" ClipboardCopyMode="ExcludeHeader">
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="UID 복사" Command="{Binding CopyTextCommand}" CommandParameter="{Binding SelectedEvent.Uid}"/>
                            <MenuItem Header="행 복사 (CSV)" Command="{Binding CopyTextCommand}" CommandParameter="{Binding SelectedEvent.CsvLine}"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="시각" Binding="{Binding Time, StringFormat=HH:mm:ss.fff}" Width="100"/>
                        <DataGridTextColumn Header="PC" Binding="{Binding Host}" Width="120"/>
                        <DataGridTemplateColumn Header="종류" Width="56">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding KindText}" Style="{StaticResource KindCell}" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="리더" Binding="{Binding DisplayName}" Width="140"/>
                        <DataGridTextColumn Header="S/N" Binding="{Binding Serial}" Width="120" FontFamily="Consolas"/>
                        <DataGridTextColumn Header="UID" Binding="{Binding Uid}" Width="190" FontFamily="Consolas"/>
                        <DataGridTextColumn Header="규격" Binding="{Binding Tech}" Width="*"/>
                        <DataGridTextColumn Header="체류(ms)" Binding="{Binding DwellMs}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Grid>
    </DockPanel>
</Window>
