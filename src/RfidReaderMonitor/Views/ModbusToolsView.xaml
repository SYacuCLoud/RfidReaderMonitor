<UserControl x:Class="RfidReaderMonitor.Views.ModbusToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:RfidReaderMonitor.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ModbusToolsViewModel}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">
            <TextBlock Style="{StaticResource Label}" TextWrapping="Wrap" Margin="0,0,0,6"
                       Text="선택한 Modbus 리더의 레지스터를 0.3초마다 읽어 원값을 보여 줍니다. 태그를 놓았을 때 Present 비트가 켜지는지, UID 가 E0 로 시작하는지 보면서 설정 탭의 주소·바이트 순서를 맞추십시오."/>

            <GroupBox Header="연결">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="장치" Style="{StaticResource Label}"/>
                    <TextBlock Grid.Column="1" Text="{Binding Endpoint}" Style="{StaticResource Value}" FontWeight="SemiBold"/>
                    <TextBlock Grid.Row="1" Text="상태" Style="{StaticResource Label}"/>
                    <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                        <TextBlock Text="{Binding LinkText}" Foreground="{Binding LinkColor}" Style="{StaticResource Value}" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding LastError}" Foreground="#B03A2E" Style="{StaticResource Value}" Margin="12,0,0,0"/>
                    </StackPanel>
                    <TextBlock Grid.Row="2" Text="마지막 폴링" Style="{StaticResource Label}"/>
                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                        <TextBlock Text="{Binding LastPollText}" Style="{StaticResource Mono}"/>
                        <TextBlock Text="{Binding ResponseText, StringFormat='   응답 {0}'}" Style="{StaticResource Value}"/>
                        <TextBlock Text="{Binding CountText}" Style="{StaticResource Value}" Foreground="#888" Margin="12,0,0,0"/>
                    </StackPanel>
                    <TextBlock Grid.Row="3" Text="폴링 주기" Style="{StaticResource Label}"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding PollText}" Style="{StaticResource Value}"/>
                    <TextBlock Grid.Row="4" Text="Present 위치" Style="{StaticResource Label}"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding PresentMap}" Style="{StaticResource Value}"/>
                    <TextBlock Grid.Row="5" Text="UID 위치" Style="{StaticResource Label}"/>
                    <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding UidMap}" Style="{StaticResource Value}"/>
                </Grid>
            </GroupBox>

            <GroupBox Header="레지스터 원값 (실시간)">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Present" Style="{StaticResource Label}"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Border CornerRadius="3" Padding="8,2" VerticalAlignment="Center">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#9E9E9E"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding PresentNow}" Value="True">
                                            <Setter Property="Background" Value="#2E7D32"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Foreground="White" FontSize="11" FontWeight="SemiBold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="없음"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding PresentNow}" Value="True">
                                                <Setter Property="Text" Value="있음"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>
                        <TextBlock Text="{Binding PresentRawText}" Style="{StaticResource Mono}" Margin="12,0,0,0"/>
                    </StackPanel>
                    <TextBlock Grid.Row="1" Text="Present 비트열" Style="{StaticResource Label}"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding PresentBitsText}" Style="{StaticResource Mono}"
                               ToolTip="비트 15 … 비트 0 순서. 레지스터 방식이면 설정한 비트 자리가 태그를 놓을 때 1 이 되어야 합니다."/>
                    <TextBlock Grid.Row="2" Text="UID 워드" Style="{StaticResource Label}"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding UidWordsText}" Style="{StaticResource Mono}"/>
                    <TextBlock Grid.Row="3" Text="UID (해석)" Style="{StaticResource Label}"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding UidBytesText}" Style="{StaticResource Mono}" FontSize="14" FontWeight="SemiBold"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding UidHint}" Style="{StaticResource Value}" Foreground="#555" TextWrapping="Wrap"/>
                </Grid>
            </GroupBox>

            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                <Button Content="재접속" Command="{Binding ReconnectCommand}" MinWidth="80" Margin="0,0,8,0"/>
                <Button Content="편집…" Command="{Binding EditInSettingsCommand}" MinWidth="90" Margin="0,0,8,0" ToolTip="등록 대화상자를 열어 주소·바이트 순서를 고칩니다. 확인 즉시 적용됩니다."/>
                <Button Content="설정 다시 읽기" Command="{Binding RefreshConfigCommand}" MinWidth="90"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
